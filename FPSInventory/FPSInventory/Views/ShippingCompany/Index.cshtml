@model IEnumerable<FPSInventory.Models.ShippingCompany>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div class="line"></div>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table id="tableFPS">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Namecompany)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phonenumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Namecompany)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phonenumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCityNavigation.Namecity)
            </td>
            <td class="white">
                <a asp-action="Edit" asp-route-id="@item.IdshippingCompany">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdshippingCompany">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdshippingCompany">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
