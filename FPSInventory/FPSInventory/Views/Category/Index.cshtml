@model IEnumerable<FPSInventory.Models.Category>

@{
    ViewData["Title"] = "Categories on file";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table id="tableFPS">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Namecategory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Index" asp-controller="Product" asp-route-categoryId="@item.Idcategory">
                    @Html.DisplayFor(modelItem => item.Namecategory)
                </a> 

            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idcategory">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.Idcategory">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
