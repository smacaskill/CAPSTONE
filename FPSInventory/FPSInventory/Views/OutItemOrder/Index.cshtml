@model IEnumerable<FPSInventory.Models.OutItemOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div class="line"></div>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table id="tableFPS">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdOutorderNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProductNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdOutorderNavigation.IdoutOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProductNavigation.Product1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdoutItemOrder">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdoutItemOrder">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdoutItemOrder">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
